// Tela de controle por voz — reconhece comandos para acionar canais.
// STUB: implementação completa na Fase 8 (speech_to_text + VoiceControlNotifier).
import 'package:flutter/material.dart';

class VoiceControlScreen extends StatelessWidget {
  const VoiceControlScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Controle por Voz')),
      body: const Center(
        child: Text('VoiceControlScreen — em construção'),
      ),
    );
  }
}
